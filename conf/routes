# Routes

GET     /snl/checkPlayerToken/:playerId         controllers.GameController.checkPlayerToken(playerId: String)
PUT     /snl/movePlayerToken/:playerId/:spaces  controllers.GameController.movePlayerToken(playerId: String, spaces: Int)
